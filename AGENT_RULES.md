# Agent 工作规范（AGENT RULES）

> **持久规范**：本项目的任何诊断、修复、实现任务，必须遵循以下纪律。
> 这是项目级约束，不是一次性 todo。每次会话开始/遇到难题时都应回顾本规范。

## 1. KB 优先（Knowledge-First）

1. **KB 优先**：遇到不懂的问题、修复很久的难题、或任何技术判断，**第一步是查 KB**（kb/ 目录），找权威文档依据，**禁止盲目猜测执行**。
2. **标注出处**：每个技术结论/修复方案，必须标注来源：
   - [KB: 路径/文件] — 知识库文档权威依据
   - [实测] — 本地代码/运行实证
   - [专家: 说明] — 外部专家确认
   - [推测] — 无权威依据的推测（须明确标注，不当权威）
3. **KB 查不到才实证**：KB 里没有的，才做实证测试，并标注"KB 未公开，实测确认"。
4. **区分权威 vs 推测**：不把推测当权威；不把单次实测当普遍规律。

## 2. 能力-文档映射校验（每个工具必须对照 KB）

> 禅道定制开发 = 基于开发文档实现。**每个 MCP 工具都必须映射到 KB 权威文档**，不允许"无文档依据"的能力。

### 校验状态

| 状态 | 含义 |
|------|------|
| ✅ doc-verified | 已对照 KB 逐字段校验 |
| ⚠️ partially-verified | 部分字段对照，部分靠实测 |
| 🔴 doc-gap | KB 无对应文档（标注"KB 未公开"+ 实测依据） |
| ❌ unvalidated | 未校验（**禁止交付**） |

映射表：`CAPABILITY_DOC_MAP.md`

### 新能力 5 步前置检查（不通过禁止写代码）

| 步骤 | 动作 | 不通过的处理 |
|------|------|-------------|
| ① KB 搜索 | 用功能关键词搜索 kb/，找到权威文档 | 找不到 → 标注 doc-gap，**停止**，向用户确认 |
| ② 读文档 | 完整阅读，提取端点/参数/响应/枚举/权限 | 文档不完整 → 标注缺失项，**停止** |
| ③ 路由确认 | 确认传统 session API 路由（KB 只有 RESTful 时，用 getActionKuid 实测） | 路由未确认 → **停止** |
| ④ 权限确认 | 确认账号有权限（KB 权限说明 + 实测 user-deny） | 权限不足 → 标注"需授权"，**停止** |
| ⑤ 写映射 | 在 CAPABILITY_DOC_MAP.md 写入映射 + 校验状态 | 未写 → **禁止提交** |

**反面案例**：
- `story_assign_execution`：没查 KB 就实现 → 路由错误。正确：先查 KB → 发现只有 productplan-link → 实现正确的工具。
- `testcase_run`：没确认路由和权限就实现 → user-deny。正确：先确认 → 发现 deny → 标注"需授权"。

### 修改能力

改任何工具逻辑 → 重新对照 KB 校验 → 更新映射表状态。

## 3. 获取请求格式的正确方式（禁止 UI 抓包）

> 需求、bug、任务等的创建/编辑/指派是临时操作，浏览器 Network 中一闪而过，**无法可靠抓取**。

### 按优先级

1. **KB 开发文档**（最高）：kb/ 中的 API 文档，包括传统 session API 表单参数文档
2. **HTML 表单分析**：代码 GET 表单页面，解析 form 字段名/类型/默认值/hidden
3. **JS 提交逻辑分析**：解析表单 JS，确认提交时字段修改/添加/验证
4. **对比法**：KB 字段 vs 表单字段 vs 实际 POST 数据，找差异

### 禁止

- ❌ 要求用户"F12 抓包"或"Copy as cURL"
- ❌ 用 UI 抓包作为唯一格式确认手段

## 4. 增量验证（一个功能 = 一次验证）

> **禁止全量验证**。每个功能独立验证，错误归属清晰。

1. **实现一个 → 验证一个**：tsc + 针对性测试 → 通过后再做下一个。
2. **验证层级**：
   - 纯类型/签名 → tsc --noEmit
   - 新工具 → tsc + tools/list 确认 +1
   - 写操作 → mock 测试（不消耗登录）
   - 登录相关 → mock + 账号空闲时单次真实验证
3. **真实环境验证是最后手段**：攒批后一次性跑，不每个功能都跑。

## 5. 通用测试实体（一个实体贯穿所有测试，仅开发环境）

> **标记总开关 `ZENTAO_MARKERS`（默认 0=关）**：交付的 MCP 就是操作**真实数据**的——
> 默认不写任何机器标记（无 `[MCP]` 标题前缀、无 `MCP-AUTO` 关键词、无颜色、无 via 尾注）。
> **只有开发环境**（agent 自测/联调）才把服务端 env 设为 `ZENTAO_MARKERS=1`，此时 create/变更
> 自动写入全套标记，供云端过滤与 `zentao_cleanup_mcp` 批量回收。
> 所有写操作测试基于**同一个自己创建的测试实体**（MCP-AUTO 标记），不碰真实数据。

### 已建立的通用测试实体

> **开源版说明**：测试实体 ID 是**每个部署各自的本地知识**，不随仓库分发。请在你的部署上按下面模板自建，并记录到本地操作备忘（建议 `~/.local/share/zentao-legacy-mcp/agent-notes.md`，不入库）。

| 类型 | 建议标题 | 要求 |
|------|---------|------|
| 需求 (story) | `[MCP] 通用测试需求` | 带 MCP-AUTO keywords（开发环境自动写入） |
| 任务 (task) | `[MCP] 通用测试任务` | 带 MCP-AUTO keywords |
| Bug | `[MCP] 通用测试Bug` | 带 MCP-AUTO keywords；steps 可放 15 种富文本标签 + 真实图片作回归基准 |
| 用例 (case) | `[MCP] 通用测试用例` | 用例功能点已开通的部署才建 |
| 版本 (build) | `[MCP] 通用测试版本` | 关联到你的测试项目/产品 |

### 规则

1. **一个类型一个实体**：不创建多个测试 story/bug/case。
2. **双重标记（硬性，前提：开发环境 `ZENTAO_MARKERS=1`）**：每个 MCP 创建的测试实体必须**同时**满足——
   - **标题带 `[MCP]` 前缀**（`ZENTAO_MARK_TITLES=1`，标记开启时的子开关）：人眼在列表里可识别
   - **keywords 带 `MCP-AUTO-{时间戳}-{操作人}` token**（所有 create handler 自动写入）：机器过滤/精确 ID 检测
   缺任何一个都算**标记不完整**，发现即补（用对应 update 工具）。
   **交付环境（未设 ZENTAO_MARKERS）不适用本规则**：MCP 创建的就是真实数据，不得补加标记。
   *历史教训：曾因标记开关语义混乱导致测试实体漏标；现引入总开关 `ZENTAO_MARKERS`（默认关=真实数据），
   标记行为只由它统一控制，消除默认值歧义。*
3. **状态流转意识**：操作会改变状态（draft→active→closed），测试前确认当前状态。
4. **不碰真实数据**：写操作只在 MCP-AUTO 标记的实体上。
5. **不自动"恢复"真实数据**：如果误改了真实数据，报告用户，由用户决定如何处理。
6. **操作前记录原始状态**：每次写操作前，先读取并记录实体的当前状态。
7. **高危删除二次确认（分级）**：
   - **MCP-AUTO 标记的实体**（标题带 `[MCP]` 或 keywords 带 `MCP-AUTO-`）：agent 可自动执行 dry_run→confirm 两步，无需用户对话确认。
   - **非 MCP 标记的真实数据**（无 `[MCP]` 前缀、无 `MCP-AUTO-` token）：**必须**在对话中向用户展示影响分析，等用户明确回复"确认删除"后才执行 `dry_run=false`。禁止 agent 自行带 confirmToken 执行。
   *适用范围：product_delete / execution_delete / productplan_delete / module_delete / build_delete / story_delete / bug_delete / task_delete。*
8. **默认值/推荐值必须用户确认**：`zentao_context` 返回的产品/项目是系统推荐值（按最近活跃排序），**不是自动填入**。agent 调用需要 productID/projectID 的工具前，必须：
   - 将推荐值（名称+ID）展示给用户
   - 等用户确认"就用这个"或指定其他值
   - 用户确认后才填入参数执行
   禁止 agent 未经确认直接使用推荐值。如果用户在会话中已明确指定过（如"在 XX 产品下建需求"），则无需重复确认。
9. **搜索/统计先收缩条件再全量获取**（2026-09-10 用户确立）：任何列表/统计/工作台类工具，先让用户给出收缩条件（productID/projectID/过滤条件），再在收缩范围内**全量获取**数据，不做时间窗口截断、不限制页数。"未关闭 = 需要处理"——工作台默认统计所有未关闭且指派给我的实体（需求 active/changed/reviewing，Bug active/resolved，任务 wait/doing/done/paused）。
10. **展示遵循 field_guide 命名规则**（2026-09-10 用户确立）：数据层保持原始值（pri=1、status=resolved），展示层用 `zentao_field_guide` 的中文标签 + 禅道官方状态名，如「严重度 1 优先级 1 Bug状态 已解决」「需求状态 激活」。不过度美化（不加"致命/紧急"别名），与禅道 UI 保持一致。
11. **字段取值实证优先**（2026-09-10 教训）：`pri=0` 表示**优先级未设置**（详情页空白标签、编辑表单 option value=0 title 为空、创建表单只有 1-4），不是"第 0 级"。遇到数据与说法不一致时，先实证（创建表单选项 + 详情页渲染 + 数据分布）再下结论；用户试探性说法也要核实，不顺着改。
12. **每个 ID 附可点击 url**（2026-09-10 用户确立）：任何工具返回实体 ID 时，**该 ID 处必须附 `url` 字段**（`https://<你的禅道域名>/<entity>-view-<id>.html`），点击直达禅道页面。不仅限于需求——story/bug/task/product/build/productplan/execution/testtask/testreport/testsuite/case/module 全覆盖。由 `viewUrl`/`moduleUrl`/`withUrl` 三个 helper 统一生成；模块无独立 view 路由，url 指向 `/module-browse-<productID>.html`。AI 展示时保留 url 供用户点击。

### 反面案例

- 一次自测创建了四个测试需求 → **应该用一个**
- 用真实需求做路由调查 → **应该用通用测试需求**
- 修改真实需求试图"恢复" → **不应该碰真实数据**

## 6. 目标对齐（完成功能后与 LoopX 对比）

> **LoopX 不是摆设**。完成功能后必须与目标对比，防止脱节。

1. **完成即对比**：每完成 1-3 个功能，读取 LoopX 目标状态，确认进展是否对齐。
2. **LoopX 待办反映真实状态**：完成的标 done，过时的取消，新增的及时添加。
3. **每轮结束一句话总结**："本轮推进了目标的哪个方面"。

## 7. 失败后的行为规则

> KB 已查 + 实测仍失败时，**停止盲猜**。

1. **已排除的原因列出来**：告诉用户"试了 A/B/C，都不行"。
2. **不再尝试第 4 种猜测**：除非有 KB 文档或新证据支持。
3. **报告用户 + 请求指导**：列出已知信息 + 未知信息 + 需要的帮助。
4. **标注 doc-gap**：在 CAPABILITY_DOC_MAP 中标注"KB 已查但实测失败，根因未定位"。

## 8. 禅道响应判断陷阱（踩坑实录，禁止重犯）

> 禅道页面里**数据文本**会污染关键词判断。以下规则全部经 2026-09-09 实测验证。

1. **deny 判断**：只能用 `user-deny` 路由跳转 或 `alert('您无权...')` / `您无权访问` 判定。**禁止**在 HTML 里搜"无权"二字——需求标题/描述里可能含该词（实例：bug-create 页 457KB，"无权"来自需求 8089 标题，误判为权限不足）。
2. **成功判断**：传统表单 POST 的成功响应多为 **HTML 重定向**（`parent.location='...'`），不是 JSON。必须同时识别两种：JSON `{result:success}` 和 HTML 重定向。返回 `created: true` 但 `newID: null` 时，用 browse/view 探测确认实体是否真的创建。
3. **静默 no-op**：表单字段与服务器端表单默认值不一致时，禅道返回 `alert('该记录可能已经被改动')`（edit）或**完全静默不改**（部分版本）。对策：
   - 字段值必须与**实际 HTML 表单默认值**一致（`lastEditedDate` 必须为 `''`）
   - `steps[]`/`stepType[]`：真实行 `stepType=step`，模板值 `item` 会触发拒绝
   - steps 在 view JSON 里是**对象 map** `{id: {desc, expect, type}}`，不是数组
4. **view JSON 空对象**：`productplan-view-{id}.json`、`build-view-{id}.json`、`project-view` 等部分路由返回 `{}`——改用 browse 接口查找实体。
5. **session 与乐观锁**：edit 类操作的乐观锁绑定"读取该记录的会话"。MCP 子进程用 ZENTAO_SID 注入时，edit 前必须 `ensureLogin(true)` 强制新会话 + 重新读取记录（已固化到 testcaseUpdate handler）。
6. **删除不生效**：story/bug/task 的 delete 路由返回"成功"但 `deleted=0`（语义未明，疑似状态限制）；plan/project 的 delete 真实生效。禁止假设删除成功，必须验证实体消失。

## KB 检索方法

1. **主题检索**：`kb/LLM_SEARCH_INDEX.json` → topicIndex（按业务概念定位）
2. **关键词检索**：`kb/LLM_SEARCH_INDEX.json` → keywordIndex
3. **grep 兜底**：`grep -rn -iE '关键词' kb/ --include='*.md'`
4. **完整指南**：`kb/KB_SEARCH.md`

## 触发场景（必须查 KB）

| 场景 | 动作 |
|------|------|
| 第一次遇到的技术问题 | 先 KB 搜索，再动手 |
| 修复很久修不好的难题 | 回 KB 找权威机制，重新审视假设 |
| 要改参数/配置/逻辑 | 先查 KB 该字段的官方定义 |
| 报错信息不明 | 先查 KB 该错误码的官方语义 |
| 不确定 API 行为 | 先查 KB 请求/响应规范 |

## 执行检查清单（每次任务前）

- [ ] 这个问题 KB 里有答案吗？
- [ ] 我的假设是权威依据还是推测？（标注来源）
- [ ] 如果要改代码，KB 对该字段的官方语义是什么？
- [ ] 写操作是否在自己的 MCP-AUTO 实体上？
- [ ] 操作前是否记录了原始状态？
- [ ] 验证方式是增量（一个功能一次）还是全量？
